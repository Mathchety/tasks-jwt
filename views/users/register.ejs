<!DOCTYPE html>
<html>
  <head>
    <title>Registrar - To-Do List</title>
    <link rel="stylesheet" href="/tailwind.css" />
  </head>
  <body>
    <div class="min-h-screen bg-gray-500 flex flex-col justify-center">
      <h2 class="text-3xl font-bold text-white text-center mb-8">Registrar</h2>
      <div class="flex flex-1 items-center justify-center">
        <div
          class="bg-white rounded-xl shadow-lg px-8 py-10 flex flex-col w-[40vw] min-w-[300px] min-h-[300px] gap-4"
        >
          <% if (typeof error !== 'undefined' && error) { %>
          <p class="text-red-600 text-center"><%= error %></p>
          <% } %>
          <form
            action="/register"
            method="POST"
            class="flex flex-col gap-4 w-full"
          >
            <label for="name" class="font-semibold">Nome:</label>
            <input
              type="text"
              name="name"
              required
              class="border rounded-md px-3 py-2"
            />
            <label for="username" class="font-semibold">Usuário:</label>
            <input
              type="text"
              name="username"
              required
              class="border rounded-md px-3 py-2"
            />
            <label for="password" class="font-semibold">Senha:</label>
            <input
              type="password"
              name="password"
              required
              class="border rounded-md px-3 py-2"
            />
            <button
              type="submit"
              class="w-full text-blue-600 font-semibold bg-blue-100 hover:bg-blue-200 rounded-md py-2 text-center transition"
            >
              Registrar
            </button>
          </form>
          <p class="mt-2">
            Já tem conta?
            <a href="/login" class="text-blue-600 hover:underline"
              >Faça login</a
            >
          </p>
        </div>
      </div>
    </div>
  </body>
</html>
