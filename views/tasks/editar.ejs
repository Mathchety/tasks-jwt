<!DOCTYPE html>
<html>
  <head>
    <title>Editar Tarefa</title>
    <link rel="stylesheet" href="/tailwind.css" />
  </head>
  <body>
    <div class="min-h-screen bg-gray-500 flex flex-col justify-center">
      <h2 class="text-3xl font-bold text-white text-center mb-8">
        Editar Tarefa
      </h2>
      <div class="flex flex-1 items-center justify-center">
        <div
          class="bg-white rounded-xl shadow-lg px-8 py-10 flex flex-col w-[40vw] min-w-[300px] min-h-[300px] gap-4"
        >
          <form
            action="/tasks/edit/<%= task.id %>"
            method="POST"
            class="flex flex-col gap-4 w-full"
          >
            <label for="title" class="font-semibold">Título:</label>
            <input
              type="text"
              name="title"
              value="<%= task.title %>"
              required
              class="border rounded-md px-3 py-2"
            />
            <label for="description" class="font-semibold">Descrição:</label>
            <textarea name="description" class="border rounded-md px-3 py-2">
<%= task.description %></textarea
            >
            <button
              type="submit"
              class="w-full text-blue-600 font-semibold bg-blue-100 hover:bg-blue-200 rounded-md py-2 text-center transition"
            >
              Salvar
            </button>
          </form>
          <a href="/tasks" class="text-blue-600 hover:underline">Voltar</a>
        </div>
      </div>
    </div>
  </body>
</html>
